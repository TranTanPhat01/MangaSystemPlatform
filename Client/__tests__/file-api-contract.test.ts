import { beforeEach, describe, expect, it, vi } from 'vitest';
const {get,post,del}=vi.hoisted(()=>({get:vi.fn(),post:vi.fn(),del:vi.fn()}));vi.mock('@/lib/api',()=>({api:{get,post,delete:del}}));import { fileApi } from '@/services/file-api';
describe('file lifecycle contract',()=>{beforeEach(()=>vi.clearAllMocks());it('uses real version, download and delete routes',async()=>{const file=new File(['v'],'version.png',{type:'image/png'});await fileApi.getFileVersions('file-1');await fileApi.createVersion('file-1',file);await fileApi.downloadFile('file-1');await fileApi.deleteFile('file-1');expect(get).toHaveBeenCalledWith('/files/file-1/versions');expect(get).toHaveBeenCalledWith('/files/file-1/download',{responseType:'blob'});expect(post).toHaveBeenCalledWith('/files/file-1/versions',expect.any(FormData),expect.any(Object));const formData=post.mock.calls[0][1] as FormData;expect(formData.get('file')).toBe(file);expect(del).toHaveBeenCalledWith('/files/file-1')})});
