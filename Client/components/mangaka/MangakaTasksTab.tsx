import { useEffect, useState } from 'react';
import { authApi, AssistantDirectoryItem } from '@/services/auth-api'; import { mangaApi } from '@/services/manga-api'; import { ChapterResponse, CreateTaskRequest, PageResponse, AnnotationResponse, SeriesResponse, TaskPriority, TaskResponse, TaskStatus } from '@/types/manga'; import { isValidNonEmptyGuid } from '@/lib/guid';
export default function MangakaTasksTab({triggerModal}:{triggerModal:(title:string,content:string)=>void}){const [series,setSeries]=useState<SeriesResponse[]>([]);const [chapters,setChapters]=useState<ChapterResponse[]>([]);const [pages,setPages]=useState<PageResponse[]>([]);const [annotations,setAnnotations]=useState<AnnotationResponse[]>([]);const [assistants,setAssistants]=useState<AssistantDirectoryItem[]>([]);const [tasks,setTasks]=useState<TaskResponse[]>([]);const [seriesId,setSeriesId]=useState('');const [chapterId,setChapterId]=useState('');const [pageId,setPageId]=useState('');const [annotationId,setAnnotationId]=useState('');const [assignedToUserId,setAssigned]=useState('');const [title,setTitle]=useState('');const [reason,setReason]=useState('');const [error,setError]=useState<string|null>(null);const refresh=async()=>{const r=await mangaApi.getMyTasks();if(!r.data.success)throw new Error(r.data.message);setTasks(r.data.data)};useEffect(()=>{void Promise.all([mangaApi.getSeries(),authApi.getAssistants()]).then(async([s,a])=>{if(!s.data.success||!a.data.success)throw new Error('Load failed');setSeries(s.data.data);setAssistants(a.data.data);await refresh()}).catch(e=>setError(e instanceof Error?e.message:'Load failed'))},[]);const create=async()=>{if(!isValidNonEmptyGuid(pageId)||!isValidNonEmptyGuid(annotationId)||!isValidNonEmptyGuid(assignedToUserId)||!title.trim())return;try{const r=await mangaApi.createTask({pageId,annotationId,assignedToUserId,title:title.trim(),priority:TaskPriority.Medium} as CreateTaskRequest);if(!r.data.success)throw new Error(r.data.message);setTitle('');setAnnotationId('');await refresh()}catch(e){setError(e instanceof Error?e.message:'Create failed')}};const approve=async(id:string)=>{try{const r=await mangaApi.approveTask(id);if(!r.data.success)throw new Error(r.data.message);await refresh()}catch(e){setError(e instanceof Error?e.message:'Approve failed')}};const revision=async(id:string)=>{if(!reason.trim())return;try{const r=await mangaApi.requestTaskRevision(id,{reason:reason.trim()});if(!r.data.success)throw new Error(r.data.message);setReason('');await refresh()}catch(e){setError(e instanceof Error?e.message:'Revision failed')}};return <main>{error&&<p role="alert">{error}</p>}<h1>Task review</h1><select aria-label="Series" value={seriesId} onChange={async e=>{const id=e.target.value;setSeriesId(id);const r=await mangaApi.getChapters(id);if(r.data.success)setChapters(r.data.data)}}><option value="">Series</option>{series.map(x=><option key={x.id} value={x.id}>{x.title}</option>)}</select><select aria-label="Chapter" value={chapterId} onChange={async e=>{const id=e.target.value;setChapterId(id);const r=await mangaApi.getPages(id);if(r.data.success)setPages(r.data.data)}}><option value="">Chapter</option>{chapters.map(x=><option key={x.id} value={x.id}>{x.title}</option>)}</select><select aria-label="Page" value={pageId} onChange={async e=>{const id=e.target.value;setPageId(id);const r=await mangaApi.getPageAnnotations(id);if(r.data.success)setAnnotations(r.data.data)}}><option value="">Page</option>{pages.map(x=><option key={x.id} value={x.id}>Page {x.pageNumber}</option>)}</select><select aria-label="Annotation" value={annotationId} onChange={e=>setAnnotationId(e.target.value)}><option value="">Annotation</option>{annotations.map(x=><option key={x.id} value={x.id}>{x.type}</option>)}</select><select aria-label="Assistant" value={assignedToUserId} onChange={e=>setAssigned(e.target.value)}><option value="">Assistant</option>{assistants.map(x=><option key={x.id} value={x.id}>{x.fullName}</option>)}</select><input aria-label="Task title" value={title} onChange={e=>setTitle(e.target.value)}/><button onClick={()=>void create()}>Create Task</button><input aria-label="Revision reason" value={reason} onChange={e=>setReason(e.target.value)}/>{tasks.map(task=><article key={task.id}><h2>{task.title}</h2>{task.latestSubmission&&<div><p>Latest: {task.latestSubmission.fileId} · {task.latestSubmission.note}</p>{task.submissionHistory?.map(s=><p key={s.id}>{s.fileId} · {s.submittedAt} · {s.status}</p>)}</div>}{task.status===TaskStatus.Submitted&&<><button onClick={()=>void approve(task.id)}>Approve</button><button onClick={()=>void revision(task.id)}>Request revision</button></>}</article>)}</main>}
